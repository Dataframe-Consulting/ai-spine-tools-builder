{
  "name": "@ai-spine/weather-tool-example",
  "version": "1.0.0",
  "description": "Weather tool example using AI Spine Tools SDK with OpenWeatherMap integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rollup -c ../../rollup.shared.js",
    "dev": "nodemon --watch src --ext ts --exec \"npm run build && node dist/index.js\"",
    "start": "node dist/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "clean": "rimraf dist coverage"
  },
  "keywords": [
    "ai-spine",
    "weather",
    "openweather",
    "api",
    "example",
    "tool"
  ],
  "author": "AI Spine Tools",
  "license": "MIT",
  "dependencies": {
    "@ai-spine/tools": "workspace:*",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@ai-spine/tools-testing": "workspace:*",
    "@types/jest": "^29.5.5",
    "@types/node": "^18.17.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}